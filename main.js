// fetch(urlB)
	// .then( i => i.json() )
	// .then( i =>
	// {
		// if(i) results.push(...i);
		// else alert('Erro, não foi possível ver sua localização!');
	// })
	// .catch( err => console.log(err) );

function Main()
{
	let $urlB = 'http://apiadvisor.climatempo.com.br/api/v1/locale/city?aulo&token=ad4675329b2e5e94fbd1cd9665888061'
	let $city = document.querySelector('form.city');
	let $display = document.querySelector('div.display');
}
